kernel vec4 test(sampler src)
{
    vec2 dc = destCoord();
    vec4 extent = samplerExtent(src);
    vec4 p = sample(src, samplerTransform(src, dc));



    return vec4(float(300 >> 4)/ 20.0, 0.1, 0.0, 1.0);

}
